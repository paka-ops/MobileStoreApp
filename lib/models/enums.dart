enum Post {
  salesperson,
  cashier,
}
enum Status {
  connected,
  disconnected,
}

enum UserType {
  employer,
  employee,
  admin,
}
enum OrderStatus{
  VALIDATED,
  DELETION_PENDING,
  UPDATED,
  CREATED,
  SCRAPPED
}